// Optional form helper for label + input + validation text.
// This could clean up login/register/add-password once frontend validation is
// added, but it is not needed while those forms are still small.

// Contains the shared footer for public-facing pages.
// Currently just a placeholder, but we can expand it with links to the guide, contact info, etc
export default function Footer() {
  return (
    <footer className="site-footer">
      <p>Footer</p>
    </footer>
  );
}
